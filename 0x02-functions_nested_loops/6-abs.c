#include"main.h"

/**
 * _abs  - computes the absolute value of an integer
 * @n: the number that's gonna be computed
 * Return: the absolute value of an entered integer
 */



int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n == 0)
{
return (n);
}
else
{
return (-n);
}
}
